[package]
name = "environment"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bollard = "0.12.0"
lazy_static = "1.4.0"
sysinfo = "0.24.5"
log = "0.4.0"

remote = { path = "../remote"}